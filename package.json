{
  "name": "lightouts",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "@testing-library/user-event": "^7.2.1",
    "emotion": "^10.0.27",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1",
    "react-three-fiber": "^4.2.12",
    "styled-components": "^5.1.1",
    "three": "^0.118.3"
  },
  "scripts": {
    "start": "react-scripts start --experimental-modules",
    "build": "npm prune && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "pre-deploy": "rm -rf docs && npm run build && mv build docs",
    "git-auto": "git add --all && git commit -m autoPush && git push"
  },
  "homepage": "/lightouts/",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {}
}
